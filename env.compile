export BBSHOME=${HOME}
export BBSUID=$(id -u)
export BBSGID=$(id -g)
export BBSUSR=${USER}
export BBSGROUP=$(id -ng)
#export WWWGID=$(id -g nginx)
#export WWWGROUP=nginx
export WWWGID=$(id -g)
export WWWGROUP=$(id -ng)
