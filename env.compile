export BBSHOME="${HOME}"
export BBSUSR="${USER}"
export BBSUID=$(id -u "${BBSUSR}")
export BBSGROUP=$(id -ng "${BBSUSR}")
export BBSGID=$(id -g "${BBSUSR}")
#export WWWGID=$(id -g nginx)
#export WWWGROUP=nginx
export WWWGID=$(id -g "${BBSUSR}")
export WWWGROUP=$(id -ng "${BBSUSR}")
